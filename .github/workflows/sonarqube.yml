name: SonarQube
on: [push, pull_request]
permissions:
  contents: read
jobs:
  sonar:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Configure SonarQube token and run npm run sonar"
